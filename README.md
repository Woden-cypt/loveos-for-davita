# loveOS — for Davita

A small interactive terminal website built for Davita.

The site is intentionally static so it can run on GitHub Pages. Edit the
`DEFAULT_SITE_CONFIG` object in `index.html` to change the window title,
footer, letter, signature, or colors.
